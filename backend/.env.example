SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# Si no quieres exponer la service role, puedes usar el anon key, pero las funciones protegidas pueden fallar.
SUPABASE_ANON_KEY=
PORT=5000
JWT_SECRET=
# Admite varias URLs separadas por coma (ej: http://localhost:3000,https://tu-sitio.netlify.app)
FRONTEND_URL=http://localhost:3000
